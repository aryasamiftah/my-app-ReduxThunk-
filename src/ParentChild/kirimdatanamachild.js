import React from 'react'

const KirimNamaChild = (props) => {
 
  
    return (
    <div>
        <h2>Nama Karyawan : {props.nama}</h2>    
        {/* <h2>firstName Nama Karyawan : {props.data.firstName}</h2>
        <h2>lastName Nama Karyawan : {props.data.lastName}</h2>   */}

    </div>
  )
}

export default KirimNamaChild;
